  # Uninstall current version
  npm uninstall -g @anthropic-ai/claude-code

  # Install latest version
  npm install -g @anthropic-ai/claude-code@latest

  # Then start with Chrome integration
  claude --chrome